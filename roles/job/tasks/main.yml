# vim: set ft=yaml ts=2 expandtab:
---

- name: daily cronjob for job "{{ job }}"
  template:
    src: backup.sh.j2
    dest: /usr/local/bin/restic.{{ repo|mandatory }}.{{ job|mandatory }}
    mode: "0755"
    backup: true

...
