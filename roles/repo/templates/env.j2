# for the hostname, configure it in .ssh/config
#
export RESTIC_CACHE_DIR={{ restic_cachedir|mandatory }}
export RESTIC_REPOSITORY={{ url|mandatory }}
export RESTIC_PASSWORD_FILE=/etc/restic.repos.d/{{ repo }}/password
